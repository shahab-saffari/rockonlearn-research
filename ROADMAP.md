# RockOnLearn — برنامه‌ی ۱۰ روز کاری

**نسخه:** ۲ · **تاریخ:** ۲ سپتامبر ۲۰۲۶
**تغییر نسبت به نسخه ۱:** یافته‌های گزارش «New-user onboarding product analysis» (۱ سپتامبر، مدیر پروژه) در بخش ۰.۵ و بخش ۶ ادغام شد.
**هدف اصلی:** افزایش درآمد · **هدف دوم:** شفاف‌تر و قابل‌استفاده‌تر کردن اپ روی iOS و اندروید

---

## بخش ۰ — چرا این برنامه این شکلیه

قبل از نوشتن این برنامه، دیتای Amplitude پروژه در بازه‌ی ۳۰ تا ۶۰ روز گذشته بررسی شد. چیزهایی که پیدا شد، اولویت‌ها را نسبت به فرض اولیه تغییر داد.

### چهار یافته‌ای که کل برنامه رویشان سوار است

**۱. پرداخت خراب نیست — کاربر پشیمان می‌شود.**

در ۳۰ روز گذشته:

| مرحله | تعداد کاربر یکتا |
|---|---|
| چک‌اوت شروع شد | ۲۲۳ |
| **کنسل شد** | **۱۹۱ (۸۶٪)** |
| خطای فنی خورد | ۷ |
| موفق شد | ۶ |

RevenueCat هم ۵ خرید اولیه را تأیید می‌کند. یعنی زیرساخت پرداخت سالم است. کاربر دکمه‌ی خرید را می‌زند، صفحه‌ی پرداخت نیتیو باز می‌شود، و در همان لحظه منصرف می‌شود. این الگوی کلاسیک **شوک قیمت** است.

**۲. جواب «چرا نمی‌خرند» از قبل جمع‌آوری شده و خوانده نشده.**

یک exit survey روی پی‌وال فعال است که ۴۱۲ نفر در ۳۰ روز آن را شروع کرده‌اند:

| دلیل | تعداد | سهم |
|---|---|---|
| گران است | ۱۲۸ | ۳۵٪ |
| سایر | ۱۱۵ | ۳۲٪ |
| هنوز آماده نیستم | ۸۶ | ۲۴٪ |
| اول می‌خواهم ببینم | ۳۳ | ۹٪ |

«گران است» دلیل اول است. «سایر» با ۳۲٪ یعنی گزینه‌های فعلی دلیل واقعی را پوشش نمی‌دهند.

**۳. اپ خانه ندارد.**

استفاده از تب‌های پایین در ۳۰ روز گذشته (کاربر یکتا):

| تب | کاربر |
|---|---|
| path | ۱۲۳ |
| speaking | ۱۱۱ |
| study | ۱۱۱ |
| profile | ۱۰۴ |
| today | ۹۳ |
| feed | ۹ |
| clips / channels | ۱ |

هشت تب وجود دارد و پنج تای اول تقریباً مساوی هستند. در یک اپ سالم، یک تب باید غالب باشد و بقیه دنباله. توزیع مساوی یعنی کاربر دارد **می‌گردد**، نه اینکه جای خودش را پیدا کرده باشد. اینکه profile سومین تب پربازدید است، این تفسیر را تقویت می‌کند: آدمی که نمی‌داند قدم بعدی چیست، سراغ تنظیمات می‌رود.

**۴. قلب محصول تقریباً لمس نمی‌شود.**

رویداد `mission_speak_started` هفته‌ای ۱ تا ۳ کاربر دارد. کل ارزش‌گزاره‌ی محصول «همان روز با AI صحبت کن» است و عملاً هیچ‌کس به آن نمی‌رسد. در مقابل `coach_lesson_completed` هفته‌ای حدود ۱۹۶ کاربر دارد. یعنی کاربر محتوا مصرف می‌کند ولی به لحظه‌ای که ارزش پولی محصول است نمی‌رسد.

### دو دارایی که از قبل وجود دارد و در برنامه از آن‌ها استفاده می‌کنیم

- **فریم‌ورک آزمایش پی‌وال** از قبل پیاده‌سازی شده (`paywall_experiment_variant`، `paywall_experiment_assigned`، `paywall_experiment_show_percent`). برای تست نسخه‌های مختلف پی‌وال نیازی به ساخت زیرساخت از صفر نیست.
- **RevenueCat** با دو اپ جدا (iOS و اندروید) وصل است. منبع حقیقت درآمد وجود دارد.

### یک هشدار مهم درباره‌ی کیفیت دیتا

تاکسونومی رویدادها تکه‌تکه است. برای «دیدن پی‌وال» شش نام موازی وجود دارد (`paywall_viewed`، `Paywall Viewed`، `funnel_paywall_viewed`، `onboarding_paywall_viewed`، `paywall_offer_viewed`، `Offer Viewed`) و برای «خرید» پنج نام. دو کانوانسیون نام‌گذاری (snake_case و Title Case) هم‌زمان زنده هستند. ضمناً رویدادهای آنبردینگ (`onboarding_flow_started` و `onboarding_flow_completed`) از حدود ۴-۵ آگوست دیگر ثبت نشده‌اند، در حالی که بقیه‌ی رویدادها تا ۲۹ آگوست فعال بوده‌اند.

**معنی عملی:** روند ماه‌به‌ماه قابل اتکا نیست، چون بخشی از «رشد» در نمودارها رشد نصب رویداد است نه رشد کاربر. فقط هفته‌های آگوست را معتبر می‌دانیم.

---

## بخش ۰.۵ — یافته‌های گزارش مدیر پروژه (۱ سپتامبر)

گزارشی جدا با عنوان «New-user onboarding product analysis» توسط مدیر پروژه تهیه شده است. اعداد آن با تحلیل مستقل ما تطبیق داده شد.

### تطبیق: هیچ مغایرت واقعی وجود ندارد

| موضوع | گزارش مدیر (۲۸ روز) | تحلیل ما (۳۰ روز) |
|---|---|---|
| شروع چک‌اوت | ۲۴۳ | ۲۲۳ |
| کنسل چک‌اوت | ۲۰۰ (۸۲٪) | ۱۹۱ (۸۶٪) |
| خرید | ۷ | ۶ موفق / ۵ تأیید RevenueCat |
| نام‌های تکراری رویداد | تأیید شده | تأیید شده |
| زنجیره‌ی coaching شکسته | تأیید شده | `onboarding_flow_*` از ۴–۵ آگوست خاموش |

اختلاف اعداد فقط ناشی از بازه‌ی متفاوت است. یعنی می‌توان با اطمینان به این اعداد در جلسات استناد کرد.

### سه چیز جدید که در تحلیل ما نبود

**۱. یک قیف وب جدا از اپ وجود دارد.**
تعریف «ورود به آنبردینگ» در گزارش `Funnel Session Started` است و مراحل آن Chat ← Name ← Native language ← Topics ← … است. این با فلومپ ما (اسلایدهای معرفی، کوییز، ریماندر، لاگین) یکی نیست. یعنی دو سطح مجزا داریم و باید در همه‌ی تحلیل‌ها از هم تفکیک شوند. تأیید مستقل: `paywall_source` سه مقدار دارد — `funnel_paywall`، `subscription_paywall`، `trial_expired`.

**۲. شکاف فعال‌سازی — بزرگ‌ترین نشت کل محصول.**
از ۲,۳۸۵ نفری که وارد قیف می‌شوند، تنها **۲۰۸ نفر (۸.۷٪) بعداً اپ را باز می‌کنند**. یعنی حدود ۹۲۱ نفر کل پرسشنامه را تا آخر پر می‌کنند و سپس ناپدید می‌شوند. این عدد از هر یافته‌ی دیگری در این سند بزرگ‌تر است و در برنامه‌ی اقدام خودِ گزارش هم به آن پرداخته نشده.

**۳. افت مرحله‌ی Chat ← Name.**
۳۶.۶٪ در ۲۸ روز (۶۹۰ کاربر)، ۴۱.۷٪ امروز. بزرگ‌ترین افت داخل پرسشنامه. بعد از عبور از Name، تکمیل بقیه‌ی مراحل قوی است.

### دو تصحیح که باید وارد بحث شود

**الف) این جمله‌ی گزارش نادرست است:** «دیتاست هیچ پاسخ نظرسنجی یا فیلد قصد خرید ندارد، پس قیمت را نمی‌توان به‌عنوان علت رتبه‌بندی کرد.»

نظرسنجی خروج پی‌وال وجود دارد و ماهانه ۴۱۲ نفر آن را پر می‌کنند: گران است ۳۵٪، سایر ۳۲٪، آماده نیستم ۲۴٪، اول می‌خواهم ببینم ۹٪. به همین دلیل قیمت‌گذاری از توصیه‌های گزارش کاملاً غایب است. این مهم‌ترین تصحیحی است که باید مطرح شود.

**ب) افت ۴۱.۷٪ در Chat ← Name باید قبل از خرج کردن اسپرینت صحت‌سنجی شود.**
اگر رویداد «Chat viewed» هنگام لود شدن صفحه به‌صورت خودکار برای همه بزند، مخرج کسر مصنوعاً بزرگ می‌شود و اولین تعامل واقعی شبیه پرتگاه به نظر می‌رسد. این یکی از رایج‌ترین خطاهای خواندن قیف است. ممکن است افت واقعی باشد، ولی یک ساعت بررسی قبل از دو روز طراحی معامله‌ی خوبی است.

### منابع گزارش

- داشبورد اصلی: `https://app.amplitude.com/analytics/rockonlearn-228283/dashboard/avv1h5qg`
- به‌جای ساختن داشبورد از صفر، همین مبنا گرفته شود.

---

## بخش ۱ — سه اصلی که در تمام ۱۰ روز رعایت می‌شود

**اصل اول: متریک موفقیت این اسپرینت «تعداد خرید» نیست.**

با ۵ تا ۶ خرید در ماه، هیچ تغییری از نظر آماری قابل اثبات نیست. اگر خرید را متریک بگذاریم، در روز ۱۰ فقط نویز می‌بینیم.

متریک اصلی: **نرخ کنسل چک‌اوت** (الان ۸۶٪). حجم این متریک ۲۲۳ نفر در ماه است — کافی برای دیدن تغییر.

متریک‌های کمکی: نرخ عبور از پی‌وال به چک‌اوت، سهم «سایر» در exit survey، تمرکز استفاده‌ی تب‌ها.

**اصل دوم: در دو موج شیپ می‌کنیم، نه یک‌جا.**

چون شیپ کردن سریع است، وسوسه‌ی شیپ کردن همه‌چیز با هم وجود دارد. اگر ده تغییر با هم برود، در روز ۱۰ نمی‌دانیم کدام اثر داشته. موج اول روز ۶، موج دوم روز ۹، با فاصله‌ی حداقل ۴۸ ساعت.

**اصل سوم: هیچ تغییری قبل از اینکه رویدادش درست باشد شیپ نمی‌شود.**

اگر رویداد شکسته باشد، شیپ سریع فقط سرعت کور شدن را زیاد می‌کند.

---

## بخش ۲ — برنامه‌ی روز به روز

هر روز چهار بخش دارد: **کار تو**، **کار دولوپر**، **خروجی مورد انتظار**، و **وضعیت تصمیم** (قطعی است یا با تو).

---

### روز ۱ — تفکیک جغرافیایی و راه‌اندازی متن آزاد

**هدف:** فهمیدن اینکه کاربر واقعی ما کجاست و قیمت فعلی نسبت به قدرت خرید او چقدر است. این تنها چیزی است که کل توصیه‌ی قیمت‌گذاری روی آن سوار است.

**کار تو:**
1. در Amplitude، کاربران فعال ۳۰ روز گذشته را بر حسب `country` تفکیک کن.
2. همان تفکیک را روی `paywall_checkout_started` و `Checkout Cancelled` انجام بده. سؤالی که باید جواب بگیرد: آیا نرخ کنسل در کشورهای با درآمد پایین‌تر بیشتر است؟
3. در RevenueCat، درآمد واقعی ۳۰ روز گذشته را به تفکیک کشور و پلن بردار.
4. عدد RevenueCat را با Amplitude مقایسه کن. اگر اختلاف زیاد بود، یعنی رویداد خرید ناقص است و باید قبل از هر کاری درست شود.
5. قیمت فعلی (۹۹ دلار سالانه) را در برابر متوسط درآمد ماهانه در سه کشور اول بگذار.

**کار دولوپر:**
- افزودن یک فیلد متن آزاد اختیاری به گزینه‌ی «سایر» در exit survey پی‌وال. کار کوچکی است ولی باید امروز شیپ شود تا تا روز ۷ داده جمع شده باشد.
- رویداد: `paywall_exit_survey_completed` با پراپرتی جدید `exit_reason_text`.

**خروجی:**
- یک جدول: کشور × کاربر × نرخ کنسل چک‌اوت × درآمد
- تأیید یا رد این فرضیه: «کنسل شدن، ناشی از قیمت نامتناسب با بازار است»
- فیلد متن آزاد فعال روی پروداکشن

**وضعیت تصمیم:** **قطعی.** فیلد متن آزاد بدون بحث باید شیپ شود؛ ۳۲٪ از پاسخ‌ها الان بی‌استفاده هستند. تفکیک جغرافیایی هم پیش‌نیاز همه‌چیز است.

---

### روز ۲ — تجربه‌ی دست‌اول و نقشه‌ی معماری اطلاعات

**هدف:** تبدیل گیج شدن شخصی به شواهد قابل ارائه، و مستند کردن هم‌پوشانی تب‌ها.

**کار تو:**
1. یک نصب کاملاً تازه روی iOS و یکی روی اندروید. از لحظه‌ی زدن آیکون تا ۱۰ دقیقه بعد ضبط ویدیو کن و با صدای بلند فکر کن.
2. هر جا مکث کردی یا ندانستی قدم بعدی چیست، همان لحظه یادداشت کن با زمان دقیق. مثال: «ثانیه ۴۰ — وارد Explore شدم، نفهمیدم اول کدام ردیف را بزنم.»
3. جدول IA را بساز. برای هر یک از هشت تب:

| تب | قرار است چه کاری بکند | عملاً چه چیزی داخلش هست | با کدام تب‌ها هم‌پوشانی دارد | کاربر ۳۰ روز |
|---|---|---|---|---|

4. از دو یا سه نفر از تیم بپرس هر تب برای چیست. اگر جواب‌ها با هم فرق داشت، همان خودش یافته است و باید ثبت شود.

**کار دولوپر:** کاری ندارد. اگر ظرفیت آزاد است، می‌تواند روی پاک‌سازی تاکسونومی (روز ۳) جلو بیفتد.

**خروجی:**
- دو ویدیوی نصب تازه + یادداشت‌های زمان‌دار
- جدول IA پرشده
- لیست تب‌هایی که ادعای یکسان دارند

**وضعیت تصمیم:** **قطعی.** بدون تجربه‌ی دست‌اول، هر توصیه‌ی UX حدس است. ضمناً ۹۰ ثانیه از این ویدیو در جلسه‌ی روز ۱۰ قانع‌کننده‌تر از هر اسلایدی خواهد بود.

---

### روز ۳ — پیشنهاد معماری قیمت‌گذاری

**هدف:** طراحی ساختار قیمتی که با شواهد روز ۱ سازگار باشد.

**کار تو:**
چهار تصمیم قیمت‌گذاری را به‌صورت پیشنهاد مکتوب دربیاور:

1. **بومی‌سازی قیمت (price localization).** برای کشورهایی که سهم کاربر بالایی دارند ولی خرید صفر است، قیمت متناسب با بازار تعریف کن. این کار کاملاً در App Store Connect و Play Console انجام می‌شود و **نیازی به ریلیز اپ ندارد**.
2. **پلن پیش‌فرض.** ساختار فعلی (۲ دلار معرفی ماهانه کنار ۹۹ دلار سالانه) لنگر ذهنی را روی ۲ دلار می‌گذارد و سالانه را غیرممکن جلوه می‌دهد. پیشنهاد: سالانه پیش‌فرض باشد با نمایش معادل هفتگی.
3. **تریال به‌جای قیمت معرفی.** تریال رایگان معمولاً بهتر از قیمت معرفی عمل می‌کند چون تصمیم پرداخت را از لحظه‌ی اول جدا می‌کند.
4. **شفافیت قبل از صفحه‌ی پرداخت.** با ۸۶٪ کنسل، کاربر در لحظه‌ی دیدن عدد واقعی جا می‌زند. یعنی عددی که قبل از آن دیده با انتظارش نخوانده. عدد نهایی با ارز محلی باید قبل از باز شدن صفحه‌ی نیتیو کاملاً واضح باشد.

**کار دولوپر:**
- تأیید فنی: آیا تغییر قیمت‌ها فقط از کنسول ممکن است یا محصولات جدید در RevenueCat لازم است؟
- انتخاب رویدادهای canonical برای پی‌وال و خرید، و مستند کردنشان. بقیه به‌عنوان deprecated علامت بخورند.

**خروجی:**
- سند پیشنهاد قیمت‌گذاری با عدد مشخص برای هر بازار
- لیست رویدادهای canonical (یک نام برای هر مفهوم)

**وضعیت تصمیم:** **با توست.**
- بومی‌سازی قیمت: قطعی توصیه می‌شود، ولی **کدام کشورها و چه عددی** تصمیم توست و به دیتای روز ۱ بستگی دارد.
- تریال در برابر قیمت معرفی: این یک تصمیم تجاری است، نه فنی. اگر جریان نقدی کوتاه‌مدت مهم است، قیمت معرفی بماند؛ اگر رشد پایه‌ی مشترکین مهم است، تریال. **این را باید با ذی‌نفع تجاری تصمیم بگیری، نه خودت.**

---

### روز ۴ — تشریح رقبا در همان بازارها

**هدف:** فهمیدن اینکه کاربر ما با چه قیمتی و چه ساختاری مقایسه می‌کند.

**کار تو:**
1. پی‌وال و ساختار قیمت این‌ها را اسکرین‌شات بگیر: Duolingo، Speak، ELSA، Praktika، Cambly.
2. مهم: قیمت‌ها را **در همان کشورهایی که کاربر داری** ببین، نه قیمت آمریکا. اگر VPN لازم است، استفاده کن.
3. ببین هر کدام قبل از درخواست پول چه مقدار ارزش نشان می‌دهند.
4. ۲۰۰ ریویوی اخیر استور (هر دو پلتفرم) را تم‌بندی کن، با تمرکز روی ۱ و ۲ ستاره‌ها و کلمات مربوط به قیمت، کنسل، و گیج شدن.

**کار دولوپر:** کاری ندارد.

**خروجی:**
- جدول مقایسه‌ی قیمت و ساختار پلن رقبا در بازارهای هدف
- تم‌بندی ریویوها

**وضعیت تصمیم:** **قطعی** که انجام شود. عمق کار به تو بستگی دارد — اگر وقت کم آمد، فقط دو رقیب نزدیک کافی است.

---

### روز ۵ — طراحی موج اول

**هدف:** آماده کردن اسپک قابل شیپ برای تغییراتی که مستقیماً به نرخ کنسل چک‌اوت می‌خورند.

**کار تو، سه آیتم:**

**۱. ویوپورت اول پی‌وال.**
مشکل ۱۰ اسکرین بودن نیست؛ مشکل این است که معامله در نگاه اول معلوم نیست. اسکرین اول باید چهار چیز داشته باشد و بس:
- پلن پیش‌فرض انتخاب‌شده
- قیمت با ارز محلی، هم به صورت کل و هم معادل هفتگی
- یک اثبات (امتیاز یا تعداد کاربر)
- یک دکمه‌ی اصلی

بقیه‌ی محتوا پایین‌تر می‌ماند برای کسی که می‌خواهد بیشتر قانع شود.

**۲. انعکاس جواب‌های کاربر در پی‌وال.**
کاربر در آنبردینگ هدف، سطح و مانعش را گفته و «Building your week» هم دیده، ولی پی‌وال هیچ‌کدام را به او برنمی‌گرداند. پی‌وال باید با جمله‌ی خود کاربر شروع شود: هدفش، سطحش، و اینکه این پلن چطور آن مانع مشخص را حل می‌کند. این ارزان‌ترین و قابل‌اعتمادترین اهرم تبدیل در کل لیست است.

**۳. شفافیت قیمت قبل از صفحه‌ی نیتیو.**
مستقیماً به آن ۸۶٪ حمله می‌کند.

**کار دولوپر:**
- بررسی فنی اسپک و تخمین. تخمین را **از خودشان بگیر، نه اینکه خودت بزنی** — هم واقعی‌تر است، هم آن‌ها را شریک تصمیم می‌کند.
- اطمینان از اینکه رویدادهای canonical روز ۳ روی نسخه‌ی جدید درست ثبت می‌شوند.

**خروجی:**
- اسپک موج اول با کپی نهایی، برای iOS و اندروید
- تخمین مهندسی

**وضعیت تصمیم:** **قطعی** برای هر سه آیتم. هر سه مستقیماً به بزرگ‌ترین نشتی قیف می‌خورند و ریسک پایینی دارند.

---

### روز ۶ — شیپ موج اول

**کار تو:**
- بررسی نهایی روی هر دو پلتفرم قبل از انتشار
- ثبت خط پایه‌ی دقیق: نرخ کنسل چک‌اوت، نرخ پی‌وال به چک‌اوت، توزیع دلایل exit survey، همه با تاریخ مشخص. **بدون این عدد، در روز ۱۰ چیزی برای مقایسه نداری.**
- اعمال قیمت‌های محلی در App Store Connect و Play Console

**کار دولوپر:** انتشار موج اول.

**خروجی:** موج اول روی پروداکشن + سند خط پایه

**وضعیت تصمیم:** **قطعی.**

**نکته:** اگر فریم‌ورک آزمایش پی‌وال اجازه می‌دهد، موج اول را روی ۵۰٪ کاربران بده تا مقایسه‌ی هم‌زمان داشته باشی. اگر حجم کم بود، روی ۱۰۰٪ بده و قبل/بعد را مقایسه کن.

---

### روز ۷ — طراحی خانه‌ی محصول

**هدف:** حل مشکلی که خودت با آن مواجه شدی و دیتا هم تأییدش کرد.

**کار تو:**

**۱. یک تب را به‌عنوان خانه انتخاب کن.**
بعد از لاگین، کاربر باید در جایی فرود بیاید که یک قدم بعدی صریح دارد، نه در فیدی که باید در آن بگردد. با توجه به ساختار فعلی، `today` منطقی‌ترین گزینه است چون از قبل مفهوم «برنامه‌ی امروز» را دارد.

**۲. تب‌ها را ادغام کن.**
هشت تب برای این محصول زیاد است. `feed`، `clips` و `channels` روی هم ۱۱ کاربر در ماه دارند — عملاً مرده‌اند. هدف: رسیدن به سه یا چهار تب.

**۳. مسیر رسیدن به قلب محصول را کوتاه کن.**
`mission_speak_started` هفته‌ای ۱ تا ۳ کاربر دارد. اگر کاربر هرگز با AI صحبت نکند، دلیلی برای خرید ندارد. باید مشخص شود از خانه‌ی جدید تا اولین مکالمه‌ی صوتی چند تپ فاصله است و آن عدد کم شود.

**۴. تور ۱۳ مرحله‌ای را حذف کن.**
آن تور علامت است نه درمان. وقتی محصولی برای توضیح خودش به ۱۳ تولتیپ نیاز دارد، خودِ محصول ناخواناست. جایگزین: راهنمای درجا در لحظه‌ی نیاز.

**کار دولوپر:** تخمین کار ادغام تب‌ها و تغییر صفحه‌ی فرود.

**خروجی:** اسپک IA جدید با ساختار تب‌های پیشنهادی و صفحه‌ی فرود بعد از لاگین

**وضعیت تصمیم:** **با توست، ولی خیلی جدی توصیه می‌شود.**
- اینکه IA باید ساده شود: از نظر من قطعی است، هم شواهد کیفی دارد هم کمّی.
- اینکه **کدام** تب‌ها ادغام شوند و کدام خانه باشد: تصمیم توست و به جدول IA روز ۲ بستگی دارد.
- ریسک را بدان: تغییر IA برای کاربران فعلی هزینه‌ی یادگیری دارد. اگر تیم محافظه‌کار است، می‌توان فقط صفحه‌ی فرود را عوض کرد و ادغام تب‌ها را به فاز بعد سپرد.

---

### روز ۸ — خواندن نتایج و طراحی موج دوم

**کار تو:**
1. موج اول را بخوان. سؤال اصلی: **نرخ کنسل چک‌اوت پایین آمد؟** به تعداد خرید نگاه نکن، حجمش برای نتیجه‌گیری کافی نیست.
2. متن‌های آزاد exit survey را که از روز ۱ جمع شده بخوان. این اولین دیتای کیفی واقعی درباره‌ی آن ۳۲٪ است.
3. بر اساس آنچه یاد گرفتی، محتوای موج دوم را نهایی کن.

**کاندیداهای موج دوم:**
- صفحه‌ی فرود جدید بعد از لاگین
- عقب انداختن لاگین و ریماندر تا بعد از اولین تجربه‌ی ارزشمند (هر دو الان قبل از اینکه کاربر چیزی چشیده باشد چیزی از او می‌خواهند)
- کوتاه کردن مسیر تا اولین مکالمه‌ی صوتی
- تنظیم دقیق قیمت بر اساس واکنش موج اول

**کار دولوپر:** آماده‌سازی موج دوم.

**خروجی:** گزارش نتایج موج اول + اسپک موج دوم

**وضعیت تصمیم:** **با توست.** محتوای موج دوم کاملاً به نتیجه‌ی موج اول بستگی دارد و الان قابل قفل کردن نیست. اگر موج اول اثر روشنی داشت، همان مسیر را عمیق‌تر کن؛ اگر نداشت، سراغ IA برو.

---

### روز ۹ — شیپ موج دوم و آدیت پریتی اندروید

**کار تو:**
- بررسی موج دوم قبل از انتشار
- آدیت پریتی اندروید، **فقط روی همان مسیری که تغییر داده‌ایم** — نه کل اپ. چک‌لیست: رفتار دکمه‌ی بک، بریدگی متن، safe area، فلوی خرید Play، رندر قیمت با ارز محلی.
- دلیل اینکه این کار الان می‌آید و نه زودتر: اندروید حدود دو برابر iOS کاربر دارد (~۴۵۰ در برابر ~۲۲۰ هفتگی). اگر سهم درآمد اندروید نامتناسب پایین باشد، همین خودش بزرگ‌ترین یافته‌ی درآمدی است.

**کار دولوپر:** انتشار موج دوم + رفع مشکلات پریتی که پیدا می‌شود.

**خروجی:** موج دوم روی پروداکشن + لیست مشکلات پریتی با اولویت

**وضعیت تصمیم:** **قطعی** برای آدیت. رفع کردن یا نکردن هر مورد به شدت آن بستگی دارد.

---

### روز ۱۰ — ارائه و بک‌لاگ فاز بعد

**کار تو:**

**۱. سند قبل/بعد.** خط پایه‌ی روز ۶ کنار عدد امروز. صادق باش — اگر چیزی اثر نداشت، همان را بگو. اعتبار تو برای فاز بعد از این می‌آید.

**۲. بک‌لاگ فاز بعد، با سایز دلاری.** برای هر آیتم: متریک فعلی، دلتای واقع‌بینانه، اثر تخمینی ماهانه بر درآمد، تخمین مهندسی. این تنها آرتیفکتی است که بعد از رفتن تو زنده می‌ماند.

**۳. سند تاکسونومی رویدادها.** لیست canonical که در روز ۳ درست شد، به‌صورت مکتوب. تیم بعد از تو از همین استفاده می‌کند.

**۴. ارائه.** ساختار پیشنهادی:
- ۹۰ ثانیه ویدیوی نصب تازه (روز ۲) — بدون توضیح، فقط نشان بده
- چهار یافته‌ی بخش ۰
- چه چیزی شیپ شد و چه اثری داشت
- سه اولویت بعدی با عدد دلاری

**نکته‌ی مهم:** ارائه‌ی روز ۱۰ نباید رونمایی باشد. از روز ۴ به بعد یافته‌ها را با ذی‌نفع‌ها در میان بگذار تا جلسه‌ی آخر فقط تأیید باشد. بیشتر پیشنهادهای درست در سازمان‌ها به این دلیل می‌میرند که یک‌باره ارائه می‌شوند.

**خروجی:** سه سند بالا + جلسه‌ی ارائه

**وضعیت تصمیم:** **قطعی.**

---

## بخش ۳ — چه کارهایی را عمداً انجام نمی‌دهیم

نوشتن این بخش به اندازه‌ی خود برنامه مهم است، چون در دو هفته وسوسه‌ی انجام همه‌چیز زیاد است.

**۱. رکروت کردن کاربر برای تست یوزبیلیتی.** با ۴۱۲ نفر که ماهانه exit survey پر می‌کنند و ۱۹۱ نفر که چک‌اوت را کنسل می‌کنند، دیتای کیفی از قبل وجود دارد و خوانده نشده. وقت دو هفته‌ای را صرف تولید دیتای جدید نکن وقتی دیتای موجود بلااستفاده مانده.

**۲. ریدیزاین کلی.** در دو هفته تمام نمی‌شود و شیپ نمی‌شود.

**۳. تست A/B با انتظار معناداری آماری روی خرید.** حجم کافی نیست. برای همین متریک اصلی نرخ کنسل چک‌اوت است.

**۴. پاک‌سازی کامل تاکسونومی.** فقط رویدادهای canonical مسیر پول را انتخاب و مستند می‌کنیم. پاک‌سازی کامل کار فاز بعد است.

**۵. آدیت پریتی کل اپ.** فقط مسیری که تغییر داده‌ایم.

---

## بخش ۴ — ریسک‌ها

| ریسک | احتمال | چه کنیم |
|---|---|---|
| تغییر قیمت درآمد کوتاه‌مدت را پایین بیاورد | متوسط | با تعداد کم مشترک فعلی، ریسک مطلق پایین است. اول روی یکی دو بازار امتحان کن. |
| موج اول اثر قابل اندازه‌گیری نداشته باشد | متوسط | خط پایه‌ی دقیق روز ۶ حیاتی است. حتی نتیجه‌ی منفی، اطلاعات است. |
| رویداد خرید ناقص باشد و همه‌ی تحلیل‌ها اعتبار نداشته باشند | پایین تا متوسط | مقایسه‌ی روز ۱ با RevenueCat دقیقاً برای همین است. |
| تغییر IA کاربران فعلی را گیج کند | متوسط | می‌توان فقط صفحه‌ی فرود را عوض کرد و ادغام تب‌ها را عقب انداخت. |
| برنامه به خاطر ظرفیت دولوپر عقب بیفتد | پایین | تیم با AI سریع شیپ می‌کند. اگر عقب افتاد، روز ۷ (IA) اولین چیزی است که به فاز بعد منتقل می‌شود. |

---

## بخش ۵ — خلاصه‌ی تصمیم‌ها

**قطعی، بدون نیاز به تصمیم تو:**
- فیلد متن آزاد در exit survey (روز ۱)
- تفکیک جغرافیایی و تطبیق با RevenueCat (روز ۱)
- ضبط نصب تازه و جدول IA (روز ۲)
- انتخاب رویدادهای canonical (روز ۳)
- بازطراحی ویوپورت اول پی‌وال (روز ۵)
- انعکاس جواب‌های آنبردینگ در پی‌وال (روز ۵)
- شفافیت قیمت قبل از صفحه‌ی نیتیو (روز ۵)
- ثبت خط پایه (روز ۶)
- آدیت پریتی اندروید روی مسیر تغییریافته (روز ۹)
- سه سند نهایی (روز ۱۰)

**نیاز به تصمیم تو دارد:**
- کدام کشورها بومی‌سازی قیمت شوند و با چه عددی (روز ۳)
- تریال یا قیمت معرفی — **این را با ذی‌نفع تجاری تصمیم بگیر** (روز ۳)
- عمق تشریح رقبا (روز ۴)
- کدام تب‌ها ادغام شوند و کدام خانه باشد (روز ۷)
- محتوای موج دوم (روز ۸)
- درصد رول‌اوت موج اول: ۵۰٪ یا ۱۰۰٪ (روز ۶)

---

## پیوست — رویدادهای کلیدی

| مفهوم | رویداد پیشنهادی به‌عنوان canonical | نام‌های موازی که باید deprecated شوند |
|---|---|---|
| دیدن پی‌وال | `paywall_viewed` | `Paywall Viewed`، `funnel_paywall_viewed`، `onboarding_paywall_viewed`، `paywall_offer_viewed`، `Offer Viewed` |
| شروع چک‌اوت | `paywall_checkout_started` | `Checkout Started`، `checkout_started` |
| کنسل چک‌اوت | `Checkout Cancelled` | `paywall_checkout_cancelled`، `checkout_cancelled` |
| خرید | `rc_initial_purchase_event` (منبع حقیقت) | `Subscription Started`، `subscription_started`، `paywall_subscription_started`، `paywall_purchase_completed`، `purchase` |
| ناوبری | `bottom_nav_clicked` با `tab_key` | — |
| نظرسنجی خروج | `paywall_exit_survey_completed` با `exit_reason` | `Exit Survey Completed` |

**نکته:** انتخاب نهایی canonical باید با تیم فنی تأیید شود — ممکن است بعضی از این نام‌ها عمداً برای قیف وب (`funnel_*`) جدا شده باشند. مقدار `paywall_source` سه حالت دارد: `funnel_paywall`، `subscription_paywall`، `trial_expired` — که نشان می‌دهد یک قیف وب جدا از اپ وجود دارد و باید در تحلیل‌ها از هم تفکیک شوند.

---

## بخش ۶ — تغییرات نسخه ۲ نسبت به نسخه ۱

این بخش دقیقاً می‌گوید کدام روزها بر اثر گزارش مدیر پروژه تغییر کرده‌اند. بقیه‌ی روزها بدون تغییر باقی می‌مانند.

### روز ۱ — دو کار اضافه شد

- **صحت‌سنجی رویداد «Chat viewed».** بررسی شود که خودکار روی لود صفحه می‌زند یا فقط با تعامل واقعی کاربر. یک ساعت کار، و تعیین می‌کند که آیا افت ۴۱.۷٪ واقعی است یا مصنوعی.
  **وضعیت تصمیم: قطعی.** قبل از هرگونه طراحی روی مرحله‌ی Name.
- **تفکیک ورودی: وب در برابر اپ.** چند درصد کاربر از قیف وب می‌آید و چند درصد مستقیم اپ را نصب می‌کند؟ این قبل از قفل کردن اسکوپ ده روزه باید معلوم باشد، وگرنه ممکن است دو هفته روی سطحی کار کنیم که فقط بخش کوچکی از ورودی از آن می‌آید.
  **وضعیت تصمیم: قطعی.**

### روز ۱ — کار دولوپر گسترش یافت

علاوه بر فیلد متن آزاد نظرسنجی خروج، این هم اضافه شود:

- **ثبت کد خطای پرداخت و علت کنسل شدن چک‌اوت.** الان تاکسونومی نمی‌تواند بین «اعتراض به قیمت»، «منصرف شدن» و «خطای فنی» تفاوت بگذارد. این توصیه از گزارش مدیر برداشته شده و مکمل فیلد متن آزاد است.
  **وضعیت تصمیم: قطعی.**

### روز ۷ — اسکوپ گسترش یافت: شکاف فعال‌سازی

طراحی «خانه‌ی محصول» حالا باید لحظه‌ی گذار از وب به اپ را هم پوشش دهد، چون بزرگ‌ترین نشت محصول همان‌جاست (۸.۷٪ فعال‌سازی).

سؤال‌هایی که باید جواب بگیرند:

1. بعد از تمام شدن پرسشنامه‌ی وب، لینک دانلود اپ چقدر واضح است؟
2. آیا کاربر بعد از نصب اپ، دوباره مجبور است همه‌چیز را از اول پر کند؟ اگر بله، این به‌تنهایی می‌تواند بیشتر افت را توضیح دهد.
3. آیا پاسخ‌های وب به اکانت اپ منتقل می‌شوند؟ (رویدادهای `funnel_onboarding_session_attach_failed` و `funnel_preferences_client_backfill_skipped` در تاکسونومی وجود دارند — یعنی این انتقال گاهی شکست می‌خورد و خودش لاگ می‌شود.)

**وضعیت تصمیم: قطعی که بررسی شود. با تو که چقدرش در همین اسپرینت اجرا شود.**

### اولویت‌های به‌روزشده

| اولویت | مورد | شواهد |
|---|---|---|
| P0 | شکاف فعال‌سازی وب ← اپ | ۲۰۸ از ۲,۳۸۵ نفر (۸.۷٪) |
| P0 | کنسل چک‌اوت | ۲۰۰ از ۲۴۳ نفر (۸۲٪) |
| P0 | ثبت علت کنسل و کد خطای پرداخت | تاکسونومی فعلی قابل تفکیک نیست |
| P1 | بومی‌سازی و ساختار قیمت | ۳۵٪ «گران است» در نظرسنجی خروج |
| P1 | مرحله‌ی Chat ← Name | ۳۶.۶٪ افت — پس از صحت‌سنجی |
| P1 | خانه‌ی محصول و ادغام تب‌ها | پنج تب با استفاده‌ی تقریباً برابر |
| P2 | حذف تور ۱۳ مرحله‌ای | علامت مشکل معماری، نه درمان آن |

### نکته‌ی روش‌شناختی

گزارش مدیر پروژه در پایان خودش می‌نویسد که این‌ها توضیح رفتاری‌اند و انگیزه‌ی درونی کاربر را اثبات نمی‌کنند. این نکته درست است و باید حفظ شود. تنها منبع فعلی ما درباره‌ی انگیزه، نظرسنجی خروج پی‌وال است — و به همین دلیل افزودن فیلد متن آزاد در روز ۱ اهمیت دوچندان پیدا می‌کند.
